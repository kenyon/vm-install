%YAML 1.1
---
# Configuration file for vm-install

# Special options, not passed to virt-install.

vm-install-options:
  templates:
    - preseed.cfg.erb
    - anaconda-ks.cfg.erb
  domain: kenyonralph.com

# Keys in the mappings below are virt-install(1) options, and values
# are the arguments to those options, just like you would specify on
# the virt-install(1) command line.

defaults: &defaults
  connect: qemu:///system
  virt-type: kvm
  memory: 4096
  cpu: host
  network: bridge=br0
  console: pty,target_type=virtio
  serial: pty
  # Need package ovmf for UEFI.
  boot: uefi

# OS defaults

debian: &debian
  <<: *defaults
  os-variant: debian9
  location: https://deb.debian.org/debian/dists/stable/main/installer-amd64/
  initrd-inject: preseed.cfg

fedora: &fedora
  <<: *defaults
  location: https://mirrors.kernel.org/fedora/releases/30/Server/x86_64/os/
  initrd-inject: anaconda-ks.cfg
  extra-args: inst.ks=file:/anaconda-ks.cfg

rhel: &rhel
  <<: *defaults
  os-variant: rhel7

# Specific VM instances

test1:
  <<: *debian
  disk: size=10,path=/data/vms/test1.sda

test2:
  <<: *fedora
  disk: size=10,path=/data/vms/test2.sda

# Local Variables:
# coding: utf-8
# End:
...
